/// AppConfig is an injectible container that delivers config information to
/// the app.
class AppConfig {
  /// apiEndpoint holds the base URL of the backend service that handles
  /// fetching.
  String apiEndpoint;
}
